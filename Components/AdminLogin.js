import styled from "styled-components";

const AdminLogin = (props) => {
    return (
        <h1>hello weddingslk</h1>
    );
};

{/* In this page, should be included Admin Login and Signup interfaces
   
    also it should be link to firebase as same approach what i have done in login and signup page for users

    when search weddingslk/admin this should be show
    note - that is the accessing way to this admin  profile 
*/}


export default AdminLogin;

